services:
  wincliagent:
    build: .
    image: wincliagent:latest
    container_name: wincliagent
    env_file:
      - .env

    volumes:
      -.:/app
    working_dir: /app

    stdin_open: true
    tty: true
    command: bash
